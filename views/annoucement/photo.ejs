<% include ./../layout/top %>

<div class="container">
  <h1>Gerir fotos do Anúncio</h1>

  <% include ./../layout/messages %>




  <form method="POST" class="form-horizontal margin-header" role="form" enctype="multipart/form-data">
    <div class="form-group">
    <input type="file" name="cska" id="idfileupdaload" multiple>
   </div>
   <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <button type="submit" class="btn btn-success" name="action" value="upload">Gravar</button>
      <a href="/annoucement/view/<%=annoucement.annouce_id%>" class="btn" >Cancelar</a>
    </div>
  </div>
</form>


<hr>

    <form method="POST" class="form-horizontal" role="form">
      <div class="form-group">
            <% if(photos != undefined || photos != null) { %>
              <% photos.forEach(function(i) { %>

                <input type="checkbox" id="<%= i %>" name="foto" value="<%= i %>"/>
                <label for="<%= i %>"><img src="/uploads/<%= i %>" class="list-edit-image"/></label> 
              <% }) %>
            <% } %>
      </div>
     <div class="form-group">
      <div class="col-sm-offset-2 col-sm-10">
        <button type="submit" class="btn btn-success" name="action" value="remove">Remover</button>
        <a href="/annoucement/view/<%=annoucement.annouce_id%>" class="btn" >Cancelar</a>
      </div>
    </div>
</form>



</div>

<% include ./../layout/bottom %>